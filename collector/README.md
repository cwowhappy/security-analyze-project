# 数据采集模块

基于 Poetry + Python 3.11 构建。

## 技术栈

- Python 3.11
- Poetry（依赖管理）
- Pydantic（数据验证）
- pytest（单元测试）

## 常用命令

```bash
# 安装依赖
poetry install

# 运行脚本
poetry run python -m stock_collector

# 运行测试
poetry run pytest
```
